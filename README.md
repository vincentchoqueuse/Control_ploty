# Python Control with Plotly

## requirements

* python-control (`pip install control`)
* plotly (`pip install plotly==4.5.4`)

## Getting Started

Start and launch the file `test.py` with jupyter notebook.
